plugins { id 'com.android.application' }
android { namespace 'bz.tikitmasta.customer'; compileSdk 35
    defaultConfig { applicationId 'bz.tikitmasta.customer'; minSdk 24; targetSdk 35; versionCode 2; versionName '1.0.1' }
    buildTypes { release { minifyEnabled false; signingConfig signingConfigs.debug } }
}
dependencies { implementation 'androidx.appcompat:appcompat:1.7.0'; implementation 'com.google.android.material:material:1.12.0' }
